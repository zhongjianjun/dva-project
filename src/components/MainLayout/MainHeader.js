import React from 'react';
import { Layout, Mune, Icon } from 'antd';
import styles from './MainLayout.css';

const { Header } = Layout;

function MainLayout() {
  return (
    <Header></Header>
  );
}

export default MainLayout;
